import {styled} from "styled-components";

export const ContainerMapCard = styled.div`
padding: 1rem;
display: grid;
grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
gap: 1rem;
justify-items: center;
background-color: #bdb5bb53;
`